import styled from 'styled-components'
import { colors, familyfonts } from '../theme'

export const Input = styled.input`
  color: ${colors.fg_primary};
  font-family: ${familyfonts};
  background: papayawhip;
  border: none;
  border-radius: 3px;
  margin-right: 1em;
  margin-top: 1em;
  margin-bottom: 1em;
  font-size: 20px;
  text-align: center;
  width: 5em;
`
